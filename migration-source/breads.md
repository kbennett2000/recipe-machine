<!--
Paste the breads-codex source markdown into this file, then run:

    php artisan recipes:migrate migration-source/breads.md --dry-run --category=auto

The --dry-run flag prints the migration plan without writing files.
Remove the flag to actually write recipes/breads/*.md output.

Contents of this directory are gitignored — source material lives
elsewhere; only the migrated output belongs in the recipes/ tree.
-->
